spring:
  application:
    name: "librarian-backend"
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: validate
server:
  port: 8081
  servlet:
    context-path: /api
management:
  endpoints:
    web:
      exposure:
        include: "*"
springdoc:
  api-docs:
    path: /docs/download
  swagger-ui:
    path: /docs
jwt:
  expiration: 3600000 # in milliseconds
